import { SearchpipecomponentPipe } from './searchpipecomponent.pipe';

describe('SearchpipecomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpipecomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
